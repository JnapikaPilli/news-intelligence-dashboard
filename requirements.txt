# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client (async)
httpx>=0.27.0

# Pydantic (FastAPI uses v2)
pydantic>=2.7.0,<3.0.0

# Vector store
faiss-cpu>=1.7.0

# Embeddings (HuggingFace sentence-transformers)
sentence-transformers>=2.7.0
# torch: use what's already installed — avoid reinstalling unless missing
# If you need GPU support swap faiss-cpu for faiss-gpu and uncomment torch[cuda]
torch>=2.0.0
transformers>=4.36.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# Utilities
python-dotenv>=1.0.0
numpy>=1.24.0